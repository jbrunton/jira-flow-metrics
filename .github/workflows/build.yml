name: Build
on: push

jobs:

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        directory: ["client", "api"]
    defaults:
      run:
        working-directory: ${{ matrix.directory }}
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm run lint
      - run: npm run build
  
